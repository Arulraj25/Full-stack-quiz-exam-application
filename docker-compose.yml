version: '3.8'

services:
  backend:
    build: ./backend
    container_name: quiz-backend
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DB_HOST=mysql
      - DB_USER=root
      - DB_PASSWORD=${DB_PASSWORD:-Arul_2025}
      - DB_NAME=${DB_NAME:-quiz_portal}
      - JWT_SECRET=${JWT_SECRET:-your_jwt_secret_key_change_this}
    ports:
      - "5000:5000"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - quiz-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  frontend:
    build: ./frontend
    container_name: quiz-frontend
    restart: always
    ports:
      - "3000:80"
    networks:
      - quiz-network
    depends_on:
      - backend

  mysql:
    image: mysql:8.0
    container_name: quiz-mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD:-Arul_2025}
      - MYSQL_DATABASE=${DB_NAME:-quiz_portal}
      - MYSQL_USER=${DB_USER:-quizuser}
      - MYSQL_PASSWORD=${DB_PASSWORD:-Arul_2025}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/1-schema.sql
    networks:
      - quiz-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${DB_PASSWORD:-Arul_2025}"]
      timeout: 20s
      retries: 10
      interval: 10s
      start_period: 30s

networks:
  quiz-network:
    driver: bridge

volumes:
  mysql_data: